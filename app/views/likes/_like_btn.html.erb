<% if current_user.like?(micropost) %>
  <% @like = current_user.likes.find_by(micropost_id: micropost) %>
  <%= link_to micropost_like_path(micropost, @like), method: :delete, remote: true, class: "like-btn text-reset", style: "text-decoration: none;" do %>
    <i class="far fa-thumbs-up"></i>
    <span class="ml-2"><%= micropost.likes.count %></span>
  <% end %>
<% else %>
  <%= link_to micropost_likes_path(micropost), method: :post, remote: true, class: "like-btn text-reset", style: "text-decoration: none;" do %>
    <i class="far fa-thumbs-up"></i>
    <span class="ml-2"><%= micropost.likes.count %></span>
  <% end %>
<% end %>