<p><%= @user.name %>さんの本棚</p>

<% @books.each do |book| %>
  <%= link_to edit_book_path(book) do %>
    <%= image_tag url_for(book.icon.variant(resize: "100x100>")) %>
    <%= book.name %>
  <% end %>
<% end %>