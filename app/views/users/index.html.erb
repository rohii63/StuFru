<% @users.each do |user| %>
  <p>
    <td><%= user.name %></td>
    <td><%= link_to 'Show', my_page_path(user) %></td>
    <td><%# link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </p>
<% end %>