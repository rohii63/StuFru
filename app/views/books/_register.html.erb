<%= form_with model: current_user.study_books.build(book_id: @book.id), url: book_registers_path, local: true do |f| %>
  <%= f.hidden_field :book_id %>
  <%= f.submit "登録", class: "btn btn-primary" %>
<% end %>